// Import libraries for making a component
import React from 'react';
import { Text } from 'react-native';

// Make a component
const Header = () => (
  <Text>Albums!</Text>
);

//Make component available elsewhere

export default Header;
